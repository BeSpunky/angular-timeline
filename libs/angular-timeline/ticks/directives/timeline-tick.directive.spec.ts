import { TimelineTickDirective } from './timeline-tick.directive';

describe('TimelineTickDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineTickDirective();
    expect(directive).toBeTruthy();
  });
});
