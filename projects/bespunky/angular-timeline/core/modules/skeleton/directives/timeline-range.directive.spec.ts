import { TimelineRangeDirective } from './timeline-range.directive';

describe('TimelineRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineRangeDirective();
    expect(directive).toBeTruthy();
  });
});
