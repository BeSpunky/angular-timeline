import { TimelineTicksDefinitionDirective } from './timeline-ticks-definition.directive';

describe('TimelineRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineTicksDefinitionDirective();
    expect(directive).toBeTruthy();
  });
});
