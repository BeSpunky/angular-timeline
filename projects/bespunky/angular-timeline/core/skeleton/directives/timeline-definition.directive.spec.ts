import { TimelineDefinitionDirective } from './timeline-definition.directive';

describe('TimelineDefinitionDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineDefinitionDirective();
    expect(directive).toBeTruthy();
  });
});
